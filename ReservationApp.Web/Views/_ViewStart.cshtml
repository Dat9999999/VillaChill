@if(User.IsInRole(SD.Role_Admin) || User.IsInRole(SD.Role_Owner) ){
    Layout = "_LayoutAdmin";
}
else if (User.IsInRole(SD.Role_Owner))
{
    Layout = "_LayoutOwner";
}
else{
    Layout = "_Layout";
}